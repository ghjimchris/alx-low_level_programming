#include <stdio.h>
/**
 * main - Entry point
 * Description: points the alphabet in lowercase\n
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
